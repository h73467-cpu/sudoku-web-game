# 開發紀錄

依時間順序記錄這個益智遊戲集的開發過程與重大決策，方便之後回顧「為什麼會長成現在這樣」。詳細規格見 `PRD.md`（本機文件，未隨репо發佈），對外的功能說明見 `README.md`。

## 2026-08-05 — 從單一數獨網站改造成遊戲集

- **架構改版**：根目錄 `index.html` 從數獨遊戲改成遊戲集首頁，數獨搬到 `/sudoku/` 子資料夾、行為完全不變（因為已經有真實使用者在玩）。localStorage 仍用同一把 key `sudoku.data`，內部改成 `{ theme, games: { <gameId>: {...} } }` 命名空間，並自動搬遷舊格式資料，不會遺失任何紀錄。
- 建立 `shared/` 共用層：`shared/style.css`（設計元件）、`shared/js/storage.js`（`GameHubStorage` 核心 + 各遊戲 storage 模組）、`shared/js/games.js`（遊戲清單設定檔，新增遊戲只要加一筆）。
- 全站字體/按鈕/圖示放大一輪，因應銀髮族使用族群（根字體 112.5%、按鈕最小高度 46px 起、首頁遊戲卡圖示 3.2rem）。
- **新增「記憶翻牌」**：翻牌配對，難度含可調百分比的超簡單、3 次提示。之後依回饋調整：配對錯誤停留時間拉長到 1.8 秒、卡牌圖示改用 CSS container query 依卡片大小等比例放大。
- **新增「1A2B 猜數字」**：猜數字＋A/B 提示。依回饋重做筆記功能：從「單一全域數字追蹤面板」改成「每欄對應一個猜測位置的候選數字表格」（跟數獨的候選數字概念一樣，一欄可記錄多個候選數字）；答案格改成點格子選取、點數字直接覆蓋該格，不用整組重打。
- **新增「打磚塊」**：一開始是固定 5 個難度＝5 關的簡化版。依回饋大改成：Web Audio 即時合成音效（可開關）、掉落道具（分裂球/板變長/機關槍/多一命等好壞道具）、無限關卡（過關自動生成更難的下一關，磚牆版型從滿版/金字塔/棋盤留空/密室四種隨機選）。另外修好一個真實 bug：滑鼠移出畫布範圍後板子會卡死不動（改成監聽整個頁面而非只監聽畫布）。
- **新增「華容道」**：經典 4×5 棋盤、10 枚棋子，難度用「反向洗牌」從已解狀態產生（保證有解），專家難度是真正的經典版面「橫刀立馬」。上線後抓到 bug：因為棋盤小、「曹」離出口的距離本來就有限，防呆機制沒檢查「只差一步就能過關」，導致每個難度都變成「曹永遠在出口正上方，一動就過關」；修好後改用「同時檢查 0 步/1 步過關＋排除獲勝走法」的方式重新洗牌，測試數百局都不再出現瞬間過關。
- **新增「推箱子」**：用「反向拉箱子」演算法從已解狀態生成關卡（保證有解），難度含可調百分比的超簡單、方向鍵/WASD/畫面十字按鈕操作、復原功能。上線前主動壓力測試就先抓到跟華容道同類型的瞬間過關問題（小棋盤中央的箱子常常完全拉不動），修好後放大最小棋盤尺寸、加上「優先移動仍卡在目標點上的箱子」的防呆機制與整局重新生成的保底重試，測試 1500 局都沒有再出現瞬間過關。

## 之前

- 2026-07-30：初版數獨網頁遊戲。
- 2026-07-31：數獨補齊 PRD 規格（每日挑戰、暫停/復原、MRV 出題演算法、5 次隨機提示、錯誤計數、遊玩紀錄/生涯總覽、主題切換）。
